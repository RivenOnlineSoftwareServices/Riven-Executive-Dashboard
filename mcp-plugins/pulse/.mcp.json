{
  "mcpServers": {
    "pulse": {
      "type": "stdio",
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server.js"],
      "env": {
        "PULSE_BEARER_TOKEN": "${user_config.bearer_token}",
        "PULSE_API_BASE_URL": "${user_config.base_url}"
      }
    }
  }
}
