{
  "mcpServers": {
    "pulse": {
      "type": "stdio",
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server.js"],
      "env": {
        "PULSE_BEARER_TOKEN": "${PLUGIN_CONFIG_bearer_token}",
        "PULSE_API_BASE_URL": "${PLUGIN_CONFIG_base_url}"
      }
    }
  }
}
