{
  "$schema": "https://code.claude.com/schemas/plugin-schema.json",
  "name": "pulse",
  "displayName": "Glowming Pulse",
  "version": "1.0.0",
  "description": "Read-only MCP access to the Glowming Pulse /v1/* analytics API (daily snapshot, weekly digest, and the nine data marts) for the Riven Dashboard 2026. Internal — Riven OSS only.",
  "author": { "name": "Riven OSS", "email": "riaan@riven.co.za" },
  "keywords": ["glowming", "pulse", "analytics", "dashboard", "riven"],
  "userConfig": {
    "bearer_token": {
      "type": "string",
      "title": "Pulse API bearer token",
      "description": "Your Pulse API bearer token, issued by Riven OSS. Stored by the plugin and never shown to the assistant.",
      "sensitive": true
    },
    "base_url": {
      "type": "string",
      "title": "Pulse API base URL",
      "description": "Production Cloud Run service. Leave as default unless pointing at a local dev instance.",
      "default": "https://pulse-api-txrwzaee2q-ew.a.run.app",
      "sensitive": false
    }
  },
  "mcpServers": "./.mcp.json"
}
